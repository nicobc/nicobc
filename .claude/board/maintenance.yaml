tickets:

  - id: T1
    title: Scrollbar + padding scroll fix
    type: fix
    scope: ui
    priority: 1
    status: READY
    description: Full viewport width is scrollable; scrollbar sits at far right of window.
    acceptance_criteria:
      - overflow-y on html/body, not on a child container
      - page scrolls when cursor hovers over left/right padding areas
      - applies to all pages
    notes:
      - CSS-only fix in global styles
